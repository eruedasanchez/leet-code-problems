const minSubArrayLen = (target: number, nums: number[]) : number => {
    let minSubArrayLength = 0;
    
    for (let i = 0; i < nums.length; i++) {
        // const element = array[i];
        
    }
    return 0;
};