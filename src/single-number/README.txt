ENG

Given a non-empty array of integers nums, every element appears 
twice except for one. Find that single one.

You must implement a solution with a linear runtime complexity and use only 
constant extra space.

Example 1:

Input: nums = [2,2,1]
Output: 1

Example 2:

Input: nums = [4,1,2,1,2]
Output: 4

Example 3:

Input: nums = [1]
Output: 1

Constraints:

1 <= nums.length <= 3 * 104
-3 * 104 <= nums[i] <= 3 * 104

Each element in the array appears twice except for one element 
which appears only once.

* ------------------------------------------------------------ *

ESP

Dada una matriz no vacía de números enteros, cada elemento aparece
dos veces excepto una. Encuentra ese único.

Debe implementar una solución con una complejidad de tiempo de ejecución lineal y utilizar únicamente
espacio adicional constante.

Ejemplo 1:

Entrada: números = [2,2,1]
Salida: 1

Ejemplo 2:

Entrada: números = [4,1,2,1,2]
Salida: 4

Ejemplo 3:

Entrada: números = [1]
Salida: 1

Restricciones:

1 <= números.longitud <= 3 * 104
-3 * 104 <= números[i] <= 3 * 104

Cada elemento de la matriz aparece dos veces excepto un elemento.
que aparece sólo una vez.

