function longestCommonPrefix(strs: string[]): string {
    let maxCommonPrefix = '';
    return maxCommonPrefix;
};

console.log(longestCommonPrefix(['hola', 'chau']));

// ["flor","flujo","vuelo"]

// 'f', 'f', 'v'

// calcular primero la long de la palabra mas chica 

// for recorriendo las letras 